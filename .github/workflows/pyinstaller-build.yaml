name: Pyinstaller Builder

permissions:
  contents: read
  packages: write

on:
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: 'Force rebuild current version'
        required: false
        default: false
        type: boolean
        
jobs:
  pyinstaller-build:
    strategy:
      matrix:
        include:
          - runner: ubuntu-latest
            arch: amd64
          - runner: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Create amd64 Executable
        uses: sayyid5416/pyinstaller@v1
        if: ${{ matrix.arch == 'amd64'}}
        with:
          python_ver: '3.12'
          python_arch: 
          spec: 'main.py'
          requirements: 'requirements.txt'
          upload_exe_with_name: 'autoserve-ts'
          options: --onefile, --name "autoserve-ts"
          
      - name: Create amd64 Executable
        uses: sayyid5416/pyinstaller@v1
        if: ${{ matrix.arch == 'arm64'}}
        with:
          python_ver: '3.12'
          python_arch: 'arm64'
          spec: 'main.py'
          requirements: 'requirements.txt'
          upload_exe_with_name: 'autoserve-ts'
          options: --onefile, --name "autoserve-ts"
